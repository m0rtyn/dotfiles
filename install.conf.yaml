- clean: ['~']

- link:
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/bin:
        path: bin/
        create: true

- brewfile:
    # This accepts the same options as `brew bundle` command:
    file: Brewfile